<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:openlp-lite="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActviity" >

    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:icon="@drawable/ic_settings"
        android:showAsAction="always"
        tools:ignore="AppCompatResource"/>

    <group android:checkableBehavior="single">
        <item android:id="@+id/sectionView"
            android:title="Columned View"/>
        <item android:id="@+id/tabbedView"
            android:title="Tabbed View" />
    </group>

    <item android:id="@+id/custom_tab_settings"
        android:title="@string/custom_tab_settings"
        tools:ignore="AppCompatResource"/>
</menu>
